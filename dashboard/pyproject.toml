[project]
name = "dashboard"
version = "0.1.0"
description = "Streamlit dashboard for Drift Triage Co-Pilot"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "streamlit>=1.40",
]
